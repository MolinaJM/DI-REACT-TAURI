# S00 · Fundamentos de Arquitectura y Configuración del Entorno (teoría)

Responde con tus palabras. No hace falta código.

1. **Virtual DOM.** Explica en 3 líneas por qué React usa un Virtual DOM y qué problema resuelve frente a manipular el DOM real directamente.
2. **Server-Side vs Client-Side.** ¿Qué diferencia hay entre el modelo tradicional (recarga completa de página) y una SPA?
3. **Ecosistema.** Para cada herramienta di qué papel juega en el curso: Node.js, npm, Vite, React, Tailwind CSS, Rust y Tauri. (Usa una tabla.)
4. **Frontend vs Backend en Tauri.** ¿Qué parte del proyecto vive en `src/` y cuál en `src-tauri/`? ¿Qué fichero configura Tauri (`tauri.conf.json`)?
5. **Dependencias.** ¿Qué diferencia hay entre `dependencies` y `devDependencies` de `package.json`? Pon un ejemplo de cada una.
6. **desinstalación/limpieza.** ¿Qué comando instala una dependencia en desarrollo (`npm install -D <paquete>`)? ¿Y ejecuta un script del proyecto (`npm run <script>`)?